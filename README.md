# Warehouse-PythonDSA
A simple Python project that simulates managing a warehouse inventory of item IDs. It supports adding, removing, searching, sorting, and running statistics (sum, max, min, median, top frequencies, etc.)
